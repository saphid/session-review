node_modules/
dist/
output/
coverage/
.env
.env.*
*.sqlite
*.sqlite-*

# Integration test fixture is committed so the suite runs without setup.
!apps/web/tests/fixtures/sessions.sqlite

# Per-machine transcript files used by the session-detail integration test.
# Regenerated by `npm run fixture:build`; absolute paths embedded in the
# fixture DB diverge per developer, so we do not commit the bodies either.
apps/web/tests/fixtures/transcripts/

.DS_Store
npm-debug.log*

# Next.js
apps/web/.next/
apps/web/out/
apps/web/.tsbuildinfo
*.tsbuildinfo

# Playwright
test-results/
playwright-report/
playwright/.cache/
